const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const read=f=>fs.readFileSync(path.join(__dirname,'..',f),'utf8');test('v0.18 assignment audio/file and rubric schema',()=>{const m=read('db/neon_upgrade_v0.18.0.sql');const r=read('src/modules/assignments/assignment.routes.js');const repo=read('src/modules/assignments/assignment.repository.js');assert.match(m,/assignment_submission_assets/i);assert.match(m,/submission_mode/i);assert.match(m,/rubric_scores JSONB/i);assert.match(r,/multer\.memoryStorage/);assert.match(r,/requireParsedCsrfToken/);assert.match(repo,/AUDIO_REQUIRED/);assert.match(repo,/findAsset/);});
