const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const read=f=>fs.readFileSync(path.join(__dirname,'..',f),'utf8');test('v0.17 skill tracking schema and integration',()=>{const m=read('db/neon_upgrade_v0.17.0.sql');const a=read('src/modules/assignments/assignment.service.js');const q=read('src/modules/questions/question.service.js');const e=read('src/modules/exams/exam.service.js');assert.match(m,/CREATE TABLE IF NOT EXISTS skills/i);assert.match(m,/student_skill_events/i);assert.match(m,/question_skills/i);assert.match(a,/recordAssignmentGrade/);assert.match(q,/setQuestionSkills/);assert.match(e,/recordExamAttempt/);});
