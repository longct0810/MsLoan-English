<%- include('../partials/app-start', { title }) %>
<div class="mb-4"><h1 class="h3 mb-1">Tài liệu học tập</h1><p class="text-secondary mb-0">Tài liệu được giáo viên chia sẻ cho <%= portal.classInfo?.name %>.</p></div>
<div class="row g-3">
  <% portal.materials.forEach(m => { %>
    <div class="col-md-6 col-xl-4">
      <div class="card material-card h-100">
        <div class="card-body p-4">
          <div class="material-type mb-3"><%= m.type %></div>
          <div class="small text-primary fw-semibold"><%= m.unit || m.lessonTitle || 'Tài liệu' %></div>
          <h2 class="h5 mt-1"><%= m.title %></h2>
          <% if (m.description) { %><div class="small text-secondary mb-2"><%= m.description %></div><% } %>
          <div class="small text-secondary mb-4">Đăng ngày <%= new Date(m.publishedAt).toLocaleDateString('vi-VN') %></div>
          <% if (m.resourceUrl) { %><a href="<%= m.resourceUrl %>" target="_blank" rel="noopener" class="btn btn-outline-primary w-100">Mở tài liệu</a><% } else { %><button class="btn btn-outline-secondary w-100" disabled>Chưa có liên kết</button><% } %>
        </div>
      </div>
    </div>
  <% }) %>
</div>
<%- include('../partials/app-end') %>
